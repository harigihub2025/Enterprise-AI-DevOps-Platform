apiVersion: v1
kind: Service
metadata:
  name: enterprise-ai-devops-service
spec:
  type: NodePort
  selector:
    app: enterprise-ai-devops
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080